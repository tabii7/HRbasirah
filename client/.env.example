# Copy to .env (client/.env) for local development.
# Production values should be set in your hosting provider (e.g. Render dashboard).

# Base URL of the backend API (no trailing slash). The client will call ${VITE_API_BASE}/api/...
VITE_API_BASE=http://localhost:5000
